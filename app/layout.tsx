import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Robert Chelala | Portfolio",
  description:
    "IT Clinical Systems Analyst & Developer specializing in data analytics, web development, and app development.",
  openGraph: {
    title: "Robert Chelala | Portfolio",
    description:
      "IT Clinical Systems Analyst & Developer specializing in data analytics, web development, and app development.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
